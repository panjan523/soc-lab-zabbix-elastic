
`./bin/kibana` - uruchomienie Kibany

`./bin/elasticsearch` - uruchomienie Elastica

`./bin/kibana-encryption-keys generate` - wygenerowanie klucza szyfrującego

`nano elasticsearch-9.5.2/kibana-9.5.2/config/kibana.ym`l - plik do konfiguracji Kibany. 

`ssh vboxuser@localhost -p 2222` - lokalne logowanie przez Windowsa do Ubuntu serwer przez port 2222 ktory dodałem w portach NAT.

`sudo elastic-agent status` - sprawdzanie czy status jest "healthy" (czy serwer jest sprawny)


`sudo ip addr flush dev eth1`
`sudo ip addr add 192.168.56.12/24 dev eth1`
`sudo ip link set eth1 up` 
-  Kali: przypisanie tymczasowe adresu IP.

`"C:\zabbix\bin\zabbix_agent2.exe" -c "C:\zabbix\conf\zabbix_agent2.conf" -t bruteforce.count`
- sprawdzanie czy zliczane są logowania

![[Pasted image 20260828011738.png]](../images/26-kali.png)

-> Wykonanie podstawowego brute-force metodą SBM.

